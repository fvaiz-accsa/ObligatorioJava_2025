/* ===========================================
   BASE GENERAL
   =========================================== */

body {
    font-family: "Segoe UI", Roboto, Arial, sans-serif;
    background: linear-gradient(135deg, #eef0ff 0%, #f4f5fb 100%);
    margin: 0;
    padding: 30px;
    color: #2b3a67;
}

h1 {
    text-align: center;
    font-size: 34px;
    font-weight: 700;
    color: #2b3a67;
    margin-bottom: 30px;
    letter-spacing: 1px;
}

/* ===========================================
   FIELDSET
   =========================================== */

fieldset {
    background-color: #ffffff;
    border: none;
    border-radius: 14px;
    padding: 25px 30px;
    width: 480px;
    margin: 0 auto;
    box-shadow: 0 6px 20px rgba(0, 0, 70, 0.08);
    transition: all 0.2s ease-in-out;
}

fieldset:hover {
    box-shadow: 0 10px 30px rgba(0, 0, 70, 0.12);
}

legend {
    font-size: 20px;
    font-weight: 600;
    padding: 0 10px;
    color: #4c6ef5;
}

/* ===========================================
   TABLA
   =========================================== */

table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 15px;
    border-radius: 12px;
    overflow: hidden;
}

table th {
    background: #4c6ef5;
    color: white;
    padding: 14px;
    font-size: 15px;
    font-weight: 600;
}

table td {
    padding: 14px;
    background: #ffffff;
    border-bottom: 1px solid #e5e7f3;
    font-size: 15px;
}

table tr:hover td {
    background: #eef0ff;
    cursor: pointer;
}

/* ===========================================
   INPUTS
   =========================================== */

input[type="text"], input[type="number"] {
    width: 100%;
    padding: 12px;
    border: 2px solid #ccd2ff;
    border-radius: 10px;
    font-size: 16px;
    transition: all 0.3s ease;
    background-color: #f8f8ff;
}

input[type="text"]:focus, 
input[type="number"]:focus {
    border-color: #7b61ff;
    background-color: #ffffff;
    box-shadow: 0 0 6px rgba(123, 97, 255, 0.3);
    outline: none;
}

/* ===========================================
   BOTONES
   =========================================== */

h\\:commandButton, input[type="submit"], button {
    padding: 12px 22px;
    background: linear-gradient(135deg, #4c6ef5, #7b61ff);
    color: white !important;
    border: none;
    border-radius: 10px;
    font-size: 16px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease-in-out;
}

h\\:commandButton:hover, 
input[type="submit"]:hover,
button:hover {
    background: linear-gradient(135deg, #3d5ae8, #6b52f0);
    box-shadow: 0 4px 12px rgba(76, 110, 245, 0.4);
}

h\\:commandButton:active,
input[type="submit"]:active,
button:active {
    transform: scale(0.97);
}

/* Botón gris (cancelar) */
.btn-secondary {
    background: #adb5bd !important;
}

.btn-secondary:hover {
    background: #949ba2 !important;
}

/* ===========================================
   LINKS
   =========================================== */

h\\:commandLink {
    color: #4c6ef5;
    font-weight: 600;
    text-decoration: none;
    transition: color 0.2s;
}

h\\:commandLink:hover {
    color: #7b61ff;
    text-decoration: underline;
}

/* ===========================================
   MENSAJES
   =========================================== */

.info-message {
    font-size: 15px;
    font-style: italic;
    color: #6c757d;
    margin-top: 12px;
    text-align: center;
}
