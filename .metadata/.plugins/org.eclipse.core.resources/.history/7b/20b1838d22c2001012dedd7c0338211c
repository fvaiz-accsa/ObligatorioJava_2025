/* ============================
   ESTILOS GENERALES
   ============================ */

body {
    font-family: "Segoe UI", Roboto, Arial, sans-serif;
    background-color: #f5f6fa;
    margin: 0;
    padding: 20px;
    color: #2c3e50;
}

h1 {
    margin-bottom: 20px;
    font-size: 28px;
    font-weight: 600;
    color: #1e2a38;
}

/* ============================
   FIELDSET
   ============================ */

fieldset {
    background-color: #ffffff;
    border: 1px solid #d5d8df;
    border-radius: 8px;
    padding: 20px;
    box-shadow: 0 2px 6px rgba(0, 0, 0, 0.06);
}

legend {
    font-size: 17px;
    font-weight: 600;
    padding: 0 6px;
    color: #34495e;
}

/* ============================
   TABLAS
   ============================ */

table {
    width: 100%;
    border-collapse: collapse;
    margin-top: 10px;
}

table th {
    background-color: #ecf0f1;
    padding: 10px;
    font-weight: 600;
    border-bottom: 1px solid #ccc;
    text-align: center;
}

table td {
    padding: 10px;
    border-bottom: 1px solid #e0e0e0;
    text-align: center;
}

table tr:hover {
    background-color: #f2f7ff;
}

/* ============================
   INPUTS
   ============================ */

input[type="text"], input[type="number"] {
    width: 100%;
    padding: 10px;
    border: 1px solid #c7cbd3;
    border-radius: 6px;
    font-size: 14px;
    background-color: #fafbff;
    box-sizing: border-box;
    transition: border-color 0.2s;
}

input[type="text"]:focus, 
input[type="number"]:focus {
    border-color: #4a90e2;
    outline: none;
    background-color: #ffffff;
    box-shadow: 0 0 4px rgba(74, 144, 226, 0.3);
}

/* ============================
   BOTONES
   ============================ */

h\\:commandButton, button, input[type="submit"] {
    padding: 10px 18px;
    background-color: #4a90e2;
    color: white;
    border: none;
    border-radius: 6px;
    font-size: 15px;
    cursor: pointer;
    transition: background 0.2s ease, transform 0.1s ease;
}

h\\:commandButton:hover, 
button:hover, 
input[type="submit"]:hover {
    background-color: #3a78c3;
}

h\\:commandButton:active, 
button:active, 
input[type="submit"]:active {
    transform: scale(0.97);
}

/* Botón secundario */
.btn-secondary {
    background-color: #7f8c8d !important;
}

.btn-secondary:hover {
    background-color: #6d7a7b !important;
}

/* ============================
   LINKS
   ============================ */

a, h\\:commandLink {
    color: #4a90e2;
    font-weight: 500;
    text-decoration: none;
}

a:hover, h\\:commandLink:hover {
    text-decoration: underline;
}

/* ============================
   MENSAJES / INFO
   ============================ */

.info-message {
    font-style: italic;
    color: #7f8c8d;
    margin-top: 10px;
}

/* ============================
   LAYOUT
   ============================ */

.container {
    max-width: 600px;
    margin: auto;
}
